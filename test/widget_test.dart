import 'package:flutter_test/flutter_test.dart';

import 'package:audio_recorder/app.dart';

void main() {
  test('AudioRecorderApp is available as the app root', () {
    expect(const AudioRecorderApp(), isA<AudioRecorderApp>());
  });
}
